<Window x:Class="DesktopHub.App.Settings.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:settings="clr-namespace:DesktopHub.App.Settings"
        Title="DesktopHub 设置"
        Width="740" Height="600" MinWidth="640" MinHeight="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Window.Resources>
        <settings:IndexEqualConverter x:Key="IndexEqual"/>
        <settings:IndexVisibilityConverter x:Key="IndexVisibility"/>

        <!-- 卡片容器:随主题换底色 -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="18,14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>

        <Style x:Key="HintText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12.5"/>
        </Style>

        <!-- 勾选控件文字随主题 -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,18,0"/>
        </Style>

        <!-- 侧边栏:分区导航(选中项圆角高亮,Segoe MDL2 图标)。
             注意 NavItem 必须先于 NavList 声明:StaticResource 不支持前向引用。 -->
        <Style x:Key="NavItem" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" CornerRadius="8" Padding="12,10" Background="Transparent" Margin="0,2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverOverlayBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource NavActiveBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NavList" TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,10"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource NavItem}"/>
        </Style>

        <!-- 拨动开关(亚克力总开关) -->
        <Style x:Key="SwitchCheck" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="Track" Width="42" Height="22" CornerRadius="11"
                                    Background="{DynamicResource SwitchOffBrush}" VerticalAlignment="Center">
                                <Border x:Name="Knob" Width="16" Height="16" CornerRadius="8" Background="#FFFFFF"
                                        HorizontalAlignment="Left" Margin="3,0,0,0"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center" Margin="9,0,0,0" RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Knob" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 细轨滑杆(不透明度):左侧填充轨道 + 圆形滑块,1% 步进 -->
        <Style x:Key="OpacitySlider" TargetType="Slider">
            <Setter Property="Height" Value="24"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="TickFrequency" Value="1"/>
            <Setter Property="IsSnapToTickEnabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center" Background="Transparent">
                            <Border Height="5" CornerRadius="2.5" Background="{DynamicResource DividerBrush}"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="5" CornerRadius="2.5" VerticalAlignment="Center"
                                                        Background="{DynamicResource AccentBrush}"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent" Height="18" VerticalAlignment="Center"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="16" Height="16" Focusable="False">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="{DynamicResource AccentBrush}" Stroke="#FFFFFF" StrokeThickness="2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20,16,20,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="172"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 侧边栏 -->
        <ListBox Grid.Column="0" Style="{StaticResource NavList}"
                 SelectedIndex="{Binding SelectedSectionIndex}">
            <ListBoxItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="个性化" FontSize="13" Margin="10,0,0,0"/>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="通用" FontSize="13" Margin="10,0,0,0"/>
                </StackPanel>
            </ListBoxItem>
            <ListBoxItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE8FD;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="分组规则" FontSize="13" Margin="10,0,0,0"/>
                </StackPanel>
            </ListBoxItem>
        </ListBox>

        <!-- 内容区:随侧边栏分区切换 -->
        <Grid Grid.Column="1" Margin="14,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <!-- 个性化 -->
                <StackPanel Visibility="{Binding SelectedSectionIndex, Converter={StaticResource IndexVisibility}, ConverterParameter=0}">
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource HintText}"
                                       Text="改动立即生效并自动保存,无需点「保存并应用」。"/>
                            <Grid Margin="0,14,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="76"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource FieldLabel}" Text="主题"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <RadioButton GroupName="ThemeMode" Content="跟随系统"
                                                 IsChecked="{Binding ThemeModeIndex, Converter={StaticResource IndexEqual}, ConverterParameter=0}"/>
                                    <RadioButton GroupName="ThemeMode" Content="深色"
                                                 IsChecked="{Binding ThemeModeIndex, Converter={StaticResource IndexEqual}, ConverterParameter=1}"/>
                                    <RadioButton GroupName="ThemeMode" Content="浅色"
                                                 IsChecked="{Binding ThemeModeIndex, Converter={StaticResource IndexEqual}, ConverterParameter=2}"/>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,14,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="76"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource FieldLabel}" Text="亚克力背景"/>
                                <CheckBox Grid.Column="1" Style="{StaticResource SwitchCheck}" Content="模糊壁纸并叠加色调"
                                          IsChecked="{Binding AcrylicEnabled}"/>
                            </Grid>

                            <Grid Margin="0,14,0,0" IsEnabled="{Binding AcrylicEnabled}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="76"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="44"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{StaticResource FieldLabel}" Text="不透明度"/>
                                <Slider Grid.Column="1" Style="{StaticResource OpacitySlider}"
                                        Value="{Binding AcrylicOpacityPercent}"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right"
                                           Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"
                                           Text="{Binding AcrylicOpacityPercent, StringFormat={} {0}%}"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- 通用 -->
                <StackPanel Visibility="{Binding SelectedSectionIndex, Converter={StaticResource IndexVisibility}, ConverterParameter=1}">
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <CheckBox Content="开机自动启动" IsChecked="{Binding StartWithWindows}"/>
                            <CheckBox Content="隐藏桌面图标(面板将成为你的桌面入口,随时可再点一次恢复)"
                                      Margin="0,10,0,0" IsChecked="{Binding HideDesktopIcons}"/>
                            <CheckBox Content="顶置胶囊岛(始终保持在其他窗口之上;关闭后可被遮挡)"
                                      Margin="0,10,0,0" IsChecked="{Binding PillTopmost}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- 分组规则 -->
                <Border Style="{StaticResource Card}" Margin="0"
                        Visibility="{Binding SelectedSectionIndex, Converter={StaticResource IndexVisibility}, ConverterParameter=2}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Style="{StaticResource HintText}" Margin="0,0,0,10"
                                   Text="关键字与路径片段用逗号分隔;保存后生效。桌面文件不会被移动,只影响面板显示。"/>
                        <DataGrid x:Name="RulesGrid" Grid.Row="1"
                                  ItemsSource="{Binding Rules}"
                                  AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True"
                                  HeadersVisibility="Column" Background="Transparent"
                                  BorderThickness="0" RowBackground="Transparent"
                                  AlternatingRowBackground="Transparent"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="{DynamicResource DividerBrush}"
                                  Foreground="{DynamicResource PrimaryTextBrush}">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverOverlayBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                    <Setter Property="Padding" Value="4,3"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource HoverOverlayBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="分组名" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                <DataGridTextColumn Header="关键字" Binding="{Binding KeywordsText, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                <DataGridTextColumn Header="路径片段" Binding="{Binding PathFragmentsText, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <!-- 操作 -->
            <WrapPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,4,0,0">
                <Button x:Name="RefreshNowButton" Content="重新扫描" Command="{Binding RefreshNowCommand}" Padding="14,6" MinWidth="88"/>
                <Button x:Name="SaveButton" Content="保存并应用" Command="{Binding SaveCommand}" Padding="14,6" MinWidth="96" Margin="8,0,0,0" FontWeight="SemiBold"/>
            </WrapPanel>
        </Grid>
    </Grid>
</Window>
